---
const { text, href = "#", className } = Astro.props
---

<a
    class={`inline-block px-6 py-2 bg-primary text-white text-sm font-neuropolitcal rounded-full shadow-md hover:bg-opacity-50 focus:outline-none focus:ring-2 focus:ring-white transition ${className}`}
    href={href}
>
    {text}
</a>
